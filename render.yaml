services:
  - type: web
    name: rubiks-ai-solver
    env: python
    buildCommand: pip install -r requirements-render.txt
    startCommand: python visualization/server.py
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
      - key: PORT
        value: 8080
